services:
  - type: web
    name: wordmajiang
    env: node
    buildCommand: cd client && npm install && npm run build && cd ..
    startCommand: node server.js
    envVars:
      - key: DEEPSEEK_API_KEY
        sync: false
      - key: PORT
        value: "3000"
